import { EntityScheduleAppointmentTime } from './entity-schedule-appointment-time.model';

describe('EntityScheduleAppointmentTime', () => {
  it('should create an instance', () => {
    expect(new EntityScheduleAppointmentTime()).toBeTruthy();
  });
});
