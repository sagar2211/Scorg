import { EntityInstructionService } from './entity-instruction-service.model';

describe('EntityInstructionService', () => {
  it('should create an instance', () => {
    expect(new EntityInstructionService()).toBeTruthy();
  });
});
