import { TemplateCategory } from './template-category.model';

describe('TemplateCategories', () => {
  it('should create an instance', () => {
    expect(new TemplateCategory()).toBeTruthy();
  });
});
