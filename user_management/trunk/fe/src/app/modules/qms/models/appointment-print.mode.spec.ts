import { AppointmentPrint } from './appointment-print.mode';

describe('AppointmentPrint', () => {
  it('should create an instance', () => {
    expect(new AppointmentPrint()).toBeTruthy();
  });
});
