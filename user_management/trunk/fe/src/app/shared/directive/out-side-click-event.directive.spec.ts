import { OutSideClickEventDirective } from './out-side-click-event.directive';

describe('OutSideClickEventDirective', () => {
  it('should create an instance', () => {
    const directive = new OutSideClickEventDirective();
    expect(directive).toBeTruthy();
  });
});
