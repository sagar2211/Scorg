import { SohwhideElementOnPermissionDirective } from './sohwhide-element-on-permission.directive';

describe('SohwhideElementOnPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new SohwhideElementOnPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
