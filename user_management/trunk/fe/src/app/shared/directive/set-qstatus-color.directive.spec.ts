import { SetQStatusColorDirective } from './set-qstatus-color.directive';

describe('SetQStatusColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetQStatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
